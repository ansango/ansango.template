---
/**
 * 🌗 Theme Element
 * 
 * @description Theme toggle button for dark/light mode switching.
 * Uses ThemeIcon and relies on theme.script.astro for functionality.
 * 
 * @usage
 * ```astro
 * <Theme />
 * ```
 * 
 * @compatible
 * - 🌗 Uses ThemeIcon component
 * - 📦 Managed by theme.script.astro
 * - 🎨 Toggles .dark class on <html>
 * - 💾 Persists preference in localStorage
 */
import { ThemeIcon } from "@/components/icons";
---

<button
  id="toggle-theme-button"
  aria-label="Toggle theme"
  class="cursor-pointer"
>
  <ThemeIcon className="size-5" />
</button>
