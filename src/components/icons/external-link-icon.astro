---
/**
 * 🔗 ExternalLinkIcon Component
 * 
 * @description Arrow pointing to top-right indicating external links.
 * Visually signifies that a link will open in a new tab/window.
 * 
 * @usage
 * ```astro
 * <ExternalLinkIcon />
 * <ExternalLinkIcon className="size-3" />
 * ```
 * 
 * @compatible
 * - 🌐 Used with external links throughout the site
 * - 🔗 Typically follows Link component with blank prop
 * - ♿ Improves accessibility and user expectations
 */
import { Svg } from "@/components/atoms";

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<Svg className={`size-4 ${className ?? ""}`}>
  <path
    fill="none"
    stroke="currentColor"
    stroke-linecap="round"
    stroke-linejoin="round"
    stroke-width="2"
    d="M7 7h10v10M7 17L17 7"></path>
</Svg>
