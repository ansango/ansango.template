---
/**
 * ✅ CheckIcon Component
 * 
 * @description Check mark icon inside a rounded square.
 * Used to indicate confirmation, completion, or selected states.
 * 
 * @usage
 * ```astro
 * <CheckIcon />
 * <CheckIcon className="size-6 text-green-500" />
 * ```
 * 
 * @compatible
 * - 🔘 Can be used in Button component
 * - ✅ Useful for forms, checklists, and success states
 * - 🎨 Supports all SVG className props
 */
import { Svg } from "@/components/atoms";

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<Svg className={className}>
  <g fill="currentColor"
    ><path
      d="M10.243 16.314L6 12.07l1.414-1.414l2.829 2.828l5.656-5.657l1.415 1.415z"
    ></path><path
      fill-rule="evenodd"
      d="M1 5a4 4 0 0 1 4-4h14a4 4 0 0 1 4 4v14a4 4 0 0 1-4 4H5a4 4 0 0 1-4-4zm4-2h14a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2"
      clip-rule="evenodd"></path></g
  >
</Svg>
