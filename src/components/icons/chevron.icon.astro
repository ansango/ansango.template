---
/**
 * ➡️ ChevronIcon Component
 * 
 * @description Right-pointing chevron arrow icon.
 * Commonly used for navigation, dropdowns, and indicating expandable content.
 * 
 * @usage
 * ```astro
 * <ChevronIcon />
 * <ChevronIcon className="chevron-right transition-transform" />
 * ```
 * 
 * @compatible
 * - 🌳 Used in TreeNode component for folder expansion
 * - 📄 Navigation menus and accordions
 * - 🔄 Can be rotated with CSS transforms
 */
import { Svg } from "@/components/atoms";

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<Svg className={className}>
  <path
    fill="currentColor"
    d="M10.586 6.343L12 4.93L19.071 12L12 19.071l-1.414-1.414L16.243 12z"
  ></path>
</Svg>
