---
/**
 * 🏷️ TagIcon Component
 * 
 * @description Hashtag/pound symbol for tags.
 * Represents content tags and categorization.
 * 
 * @usage
 * ```astro
 * <TagIcon />
 * <TagIcon className="size-4" />
 * ```
 * 
 * @compatible
 * - 🏷️ Used in Tag component
 * - 📝 Precedes tag names in content metadata
 * - 🔍 Links to tag archive pages
 */
import { Svg } from "@/components/atoms";

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<Svg className={className}>
  <path
    fill="currentColor"
    fill-rule="evenodd"
    d="M8 4v4H4v2h4v4H4v2h4v4h2v-4h4v4h2v-4h4v-2h-4v-4h4V8h-4V4h-2v4h-4V4zm6 10v-4h-4v4z"
    clip-rule="evenodd"></path>
</Svg>
