---
/**
 * ✔️ CopiedIcon Component
 * 
 * @description Simple checkmark icon indicating successful copy action.
 * Displayed when content has been copied to clipboard.
 * 
 * @usage
 * ```astro
 * <CopiedIcon />
 * <CopiedIcon className="size-4 text-green-500" />
 * ```
 * 
 * @compatible
 * - 📋 Used in Share component for copy feedback
 * - ✅ Paired with CopyIcon for toggle states
 * - 📝 Clipboard operations
 */
import { Svg } from "@/components/atoms";

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<Svg className={className}>
  <path
    fill="currentColor"
    d="m10.586 13.414l-2.829-2.828L6.343 12l4.243 4.243l7.07-7.071l-1.413-1.415z"
  ></path>
</Svg>
