---
/**
 * 📋 CopyIcon Component
 * 
 * @description Paperclip/attachment icon representing copy action.
 * Used to indicate copy-to-clipboard functionality.
 * 
 * @usage
 * ```astro
 * <CopyIcon />
 * <CopyIcon className="size-4" />
 * ```
 * 
 * @compatible
 * - 📤 Used in Share component for URL copying
 * - ✔️ Pairs with CopiedIcon for state feedback
 * - 🔘 Typically inside Button component
 */
import { Svg } from "@/components/atoms";

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<Svg className={className}>
  <path
    fill="currentColor"
    d="M14 0a5 5 0 0 1 5 5v12a7 7 0 1 1-14 0V9h2v8a5 5 0 0 0 10 0V5a3 3 0 1 0-6 0v12a1 1 0 1 0 2 0V6h2v11a3 3 0 1 1-6 0V5a5 5 0 0 1 5-5"
  ></path>
</Svg>
