---
/**
 * 🏷️ Tag Component
 * 
 * @description Component to display tags with icon and link.
 * Primarily used for content categorization.
 * 
 * @usage
 * ```astro
 * <Tag href="/tags/astro">#astro</Tag>
 * <Tag href="/tags/web-dev">#web-dev</Tag>
 * ```
 * 
 * @compatible
 * - 🎨 Includes TagIcon automatically
 * - 📝 Used in tag lists and entry metadata
 * - 🔍 Integrates with site's tag system
 */
import { TagIcon } from "@/components/icons";

export interface Props {
  href?: string;
}

const { href } = Astro.props;
---

<a class="flex items-center" href={href}>
  <TagIcon />
  <slot />
</a>
