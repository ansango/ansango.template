---
/**
 * 📦 Container Component
 * 
 * @description Responsive container with max-width and automatic centering.
 * Sets an optimal reading width (60ch) and horizontal padding.
 * 
 * @usage
 * ```astro
 * <Container>
 *   <p>Centered content</p>
 * </Container>
 * 
 * <Container element="article" className="py-8">
 *   <h1>Article</h1>
 * </Container>
 * ```
 * 
 * @compatible
 * - 🎯 Base for all main layouts
 * - 📄 Used in Default layout
 * - 🔄 Compatible with any child content
 */
interface Props {
  className?: string;
  element?: "div" | "article" | "main" | "section";
}
const cn = `px-4 max-w-[60ch] mx-auto w-full ${Astro.props.className}`;
const Element = Astro.props.element || "div";
---

<Element class={cn}>
  <slot />
</Element>
